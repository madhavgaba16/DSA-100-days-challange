#include<iostream>
using namespace std;
#include<limits.h>
int main(){
     int arr[]={1,2,3,4,5};
     int size=sizeof(arr)/sizeof(int);
      int min=INT_MAX;
      for(int i=0;i<size;i++){
        if (min>arr[i]){
        min=arr[i];
        }
      }
   cout<<min;
   return 0;
}
